# Documentation

The index of `docs/`. One row per document.

| Path | Contents |
| --- | --- |
| `docs/environment-changes.md` | The authoring tests a new environment rule must pass. |
| `docs/environment.md` | What the sandbox is, and what has been established about it. |
| `docs/runbook.md` | Deploying a change, verifying a session, and the before-commit procedure. |
| `docs/scripts.md` | What each script does, and the facts behind the shape of each step. |
